@model List<Product>

@{
    ViewData["Title"] = "Home Page";
	ViewBag.PageName = "Home";
}
<div class="slider">
    <div class="callbacks_container">
        <ul class="rslides" id="slider">
            <li>
                <a href="Product/Index"></a>
                <div class="banner1">
                    <div class="banner-info">
                        <h3>Không gian sáng bừng – Ngôi nhà thêm ấm áp</h3>
                        <p>Chúng tôi mang đến những mẫu đèn hiện đại, tinh tế và phù hợp với mọi phong cách nội thất.</p>
                    </div>
                </div>
            </li>
                <a href="Product/Index"></a>
                <div class="banner2">
                    <div class="banner-info">
                        <h3>Đèn trang trí đa dạng, giá cả hợp lý</h3>
                        <p>Từ phòng khách, phòng ngủ đến quán café, nhà hàng – chúng tôi có đầy đủ lựa chọn cho bạn.</p>
                    </div>
                </div>
            </li>
            <li>
                <a href="Product/Index"></a>
                <div class="banner3">
                    <div class="banner-info">
                        <h3>Uy tín – Chất lượng – Tận tâm</h3>
                        <p>Mỗi sản phẩm đều được chọn lọc kỹ càng, cam kết bền đẹp và an toàn khi sử dụng.</p>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</div>

<div class="items">
    <div class="container">
        <div class="items-sec">
            <div class="row">
                @foreach (var item in Model.Take(8))
                {
                    <div class="col-md-3 feature-grid">
                        <img src="@item.ProductImage" alt="@item.ProductName" style="width:70%; height:200px; object-fit:cover;" />
                        <div class="arrival-info">
                            <h4>@item.ProductName</h4>
                            <p>@item.DiscountPrice.ToString("N0") ₫</p>
                            <span class="pric1"><del>@item.Price.ToString("N0") ₫</del></span>
                            <span class="disc">
                                [@((int)(100 - (item.DiscountPrice / item.Price * 100)))% Off]
                            </span>
                        </div>
                        <div class="viw">
                            <a href="/Product/detail/@item.ProductID"><span class="glyphicon glyphicon-eye-open"></span> View</a>
                        </div>
                    </div>
                }
            </div>
        </div>
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">

                <!-- Previous -->
                <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link" href="@(ViewBag.CurrentPage == 1 ? "#" : $"?page={ViewBag.CurrentPage - 1}")" tabindex="@(ViewBag.CurrentPage == 1 ? "-1" : "0")" aria-disabled="@(ViewBag.CurrentPage == 1 ? "true" : "false")">
                        Previous
                    </a>
                </li>

                <!-- Số trang -->
                @for (int i = 1; i <= ViewBag.TotalPages; i++)
                {
                    <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                        <a class="page-link" href="?page=@i">@i</a>
                    </li>
                }

                <!-- Next -->
                <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                    <a class="page-link" href="@(ViewBag.CurrentPage == ViewBag.TotalPages ? "#" : $"?page={ViewBag.CurrentPage + 1}")" tabindex="@(ViewBag.CurrentPage == ViewBag.TotalPages ? "-1" : "0")" aria-disabled="@(ViewBag.CurrentPage == ViewBag.TotalPages ? "true" : "false")">
                        Next
                    </a>
                </li>

            </ul>
        </nav>
    </div>
</div>

